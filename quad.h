#ifndef QUAD_H
#define QUAD_H
#pragma once
#include <vector>
#include <fstream>

enum class num_of_sol {
	ZERO,
	ONE_LINEAR,
	ONE_PARABOLIC,
	TWO,
	INF
};

void quad_sol();

# endif 
